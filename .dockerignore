# Keep the build context small and the image cache stable.
.git
build*/
verification_data*/
doc/
*.pptx
*.pdf
.claude/
.vscode/
.idea/
# Python venvs / caches (host binaries; the container installs its own).
**/.venv/
**/__pycache__/
**/*.pyc
experiments/**/xags/